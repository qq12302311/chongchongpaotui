# 价格输入框严格数字验证功能实现说明

## 功能概述

在 `adminEnd/userSettings/taskSettings.vue` 中实现了严格的价格输入框验证功能，**只允许输入数字、小数点和负数**，完全过滤其他字符，为管理员提供精确的价格设置能力。

## 主要修改内容

### 1. 修改的价格输入框

以下价格输入框已从 `type="digit"` 修改为 `type="text"` 并添加了负数输入支持：

#### 主项服务价格
- **补宝基础设备费用（元）**: `bubao_base_device_fee`
- **补宝额外设备费用（元/个）**: `bubao_extra_device_fee`

#### 服务时效价格
- **时间限制费用**: `bubao_time_limit_${time.index}_fee`
  - 支持1-10个时间段的费用设置
  - 每个时间段都可以设置负数费用

#### 距离费用价格
- **基础距离费用（元）**: `bubao_base_distance_fee`
- **额外距离费用（元/公里）**: `bubao_extra_distance_fee`

### 2. 输入框修改详情

#### 修改前
```vue
<input type="digit" v-model="currentSettings.bubao_base_device_fee" class="item-input" />
```

#### 修改后
```vue
<input type="text" v-model="currentSettings.bubao_base_device_fee" class="item-input"
    @input="validateNumberInput($event, 'bubao_base_device_fee')"
    placeholder="数字/小数/负数" />
```

### 3. 新增输入验证方法

#### validateNumberInput(event, fieldName)
```javascript
validateNumberInput(event, fieldName) {
    let value = event.detail.value;
    let originalValue = value;

    // 第一步：只保留数字、负号和小数点
    value = value.replace(/[^-\d.]/g, '');

    // 第二步：处理负号 - 只能在开头出现一次
    if (value.includes('-')) {
        const negativeCount = (value.match(/-/g) || []).length;
        if (negativeCount > 1 || value.indexOf('-') > 0) {
            value = value.replace(/-/g, '');
            if (originalValue.charAt(0) === '-') {
                value = '-' + value;
            }
        }
    }

    // 第三步：处理小数点 - 只能有一个
    const dotCount = (value.match(/\./g) || []).length;
    if (dotCount > 1) {
        const firstDotIndex = value.indexOf('.');
        const beforeDot = value.substring(0, firstDotIndex + 1);
        const afterDot = value.substring(firstDotIndex + 1).replace(/\./g, '');
        value = beforeDot + afterDot;
    }

    // 第四步：处理特殊情况
    value = value.replace(/--+/g, '-');
    if (value.includes('-.')) {
        value = value.replace('-.', '-0.');
    }

    // 第五步：验证最终格式
    const finalRegex = /^-?(\d+\.?\d*|\.\d+)?$/;
    if (!finalRegex.test(value)) {
        if (value === '-') {
            // 只有负号，保留
        } else if (value === '.') {
            value = '0.';
        } else if (value === '-.') {
            value = '-0.';
        } else {
            value = '';
        }
    }

    // 更新值
    this.currentSettings[fieldName] = value;
}
```

## 技术实现细节

### 1. 输入类型变更
- **原来**: `type="digit"` - 只允许输入数字，不支持负号
- **现在**: `type="text"` - 允许输入任意字符，通过验证方法控制格式

### 2. 严格输入验证规则
- **严格字符过滤**: 只允许数字(0-9)、负号(-)、小数点(.)，其他字符一律过滤
- **负号规则**:
  - 只能出现在字符串开头
  - 最多只能有一个负号
  - 不允许连续多个负号
- **小数点规则**:
  - 最多只能有一个小数点
  - 自动处理多个小数点的情况
  - 支持 `.5` 这样的简写形式（自动转换为 `0.5`）
- **实时验证**: 每次输入都会立即触发验证和格式化
- **智能修复**: 自动修复常见的输入错误

### 3. 用户体验优化
- **占位符提示**: 显示"数字/小数/负数"明确告知允许的输入类型
- **无感知过滤**: 输入非法字符时自动过滤，用户感觉不到阻断
- **智能补全**: 自动补全不完整的输入（如 `.` 转换为 `0.`）
- **格式保持**: 保持原有的输入框样式和布局

## 支持的输入格式

### 有效输入示例
- `5` - 正整数
- `-5` - 负整数
- `5.5` - 正小数
- `-5.5` - 负小数
- `0` - 零
- `-0` - 负零
- `.5` - 省略整数部分的小数
- `-.5` - 省略整数部分的负小数

### 无效输入自动处理
- `5-` → `5` - 负号不在开头，自动移除
- `5.5.5` → `5.5` - 多个小数点，保留第一个
- `5a` → `5` - 包含字母，字母自动过滤
- `5+` → `5` - 包含其他符号，符号自动过滤
- `--5` → `-5` - 多个负号，只保留一个
- `-.` → `-0.` - 负号加小数点，自动补全为-0.
- `.` → `0.` - 单独的小数点，自动补全为0.
- `abc` → `` - 纯字母，完全过滤为空
- `5.` → `5.` - 保留，允许用户继续输入小数部分

## 数据存储和处理

### 1. 数据格式
- 所有价格数据以字符串形式存储在 `currentSettings` 对象中
- 支持负数、正数、小数等格式
- 空值处理：允许空字符串，在保存时会进行相应处理

### 2. 默认值设置
在 `providerSettings` 中已经包含了负数示例：
```javascript
bubao_time_limit_9_fee: '-2'  // 72小时内的费用设置为-2元
```

### 3. 数据验证
- 前端验证：实时输入格式验证
- 后端验证：保存时会进行数据类型转换和验证

## 业务场景应用

### 1. 折扣设置
- 设置负数价格表示折扣或减免
- 例如：`-2` 表示减免2元

### 2. 时效奖励
- 长时间服务可以设置负数费用作为奖励
- 例如：72小时内完成设置为 `-2` 元

### 3. 距离优惠
- 特定距离范围内可以设置负数费用
- 例如：基础距离内免费，设置为 `0` 或负数

## 兼容性说明

### 1. 向后兼容
- 原有的正数价格设置完全兼容
- 不影响现有的数据格式和处理逻辑

### 2. 数据迁移
- 无需特殊的数据迁移
- 现有数据可以直接使用新的输入框

### 3. 接口兼容
- 后端接口需要支持负数价格的处理
- 前端发送的数据格式保持不变

## 注意事项

### 1. 业务逻辑
- 需要确保业务逻辑能够正确处理负数价格
- 计算总价时需要考虑负数的影响

### 2. 显示处理
- 在订单显示时需要正确处理负数价格的显示
- 可能需要特殊的UI提示来区分折扣和正常费用

### 3. 数据校验
- 建议在保存时添加业务规则校验
- 例如：某些费用项目不允许为负数

## 验证逻辑测试用例

### 基础数字输入测试
| 输入 | 输出 | 说明 |
|------|------|------|
| `123` | `123` | 正整数，正常通过 |
| `-123` | `-123` | 负整数，正常通过 |
| `12.34` | `12.34` | 正小数，正常通过 |
| `-12.34` | `-12.34` | 负小数，正常通过 |

### 特殊格式处理测试
| 输入 | 输出 | 说明 |
|------|------|------|
| `.5` | `0.5` | 自动补全整数部分 |
| `-.5` | `-0.5` | 负数自动补全整数部分 |
| `.` | `0.` | 单独小数点自动补全 |
| `-.` | `-0.` | 负号加小数点自动补全 |
| `-` | `-` | 单独负号保留，允许继续输入 |

### 错误输入过滤测试
| 输入 | 输出 | 说明 |
|------|------|------|
| `12abc34` | `1234` | 过滤字母，保留数字 |
| `12.34.56` | `12.3456` | 多个小数点，保留第一个 |
| `--123` | `-123` | 多个负号，只保留一个 |
| `12-34` | `1234` | 中间的负号被移除 |
| `12+34` | `1234` | 加号被过滤 |
| `12*34` | `1234` | 特殊符号被过滤 |

### 复杂情况处理测试
| 输入 | 输出 | 说明 |
|------|------|------|
| `-12.34abc.56` | `-12.3456` | 综合处理：保留负号、数字、第一个小数点 |
| `abc-12.34` | `-12.34` | 开头字母过滤，保留有效数字 |
| `12.34-` | `12.34` | 末尾负号被移除 |
| `---.123` | `-0.123` | 多个负号和小数点的综合处理 |

## 测试建议

### 1. 功能测试
- **基础输入测试**: 验证数字、小数、负数的正常输入
- **边界值测试**: 测试极大、极小数值的处理
- **特殊格式测试**: 验证 `.5`、`-.5` 等特殊格式的自动补全

### 2. 用户体验测试
- **实时过滤测试**: 验证非法字符的即时过滤效果
- **输入流畅性测试**: 确保过滤不影响正常输入体验
- **提示信息测试**: 验证占位符提示的准确性

### 3. 数据完整性测试
- **保存读取测试**: 验证负数价格的正确保存和读取
- **页面刷新测试**: 确保数据在页面刷新后正确显示
- **格式一致性测试**: 验证不同输入方式得到的相同数值格式一致

### 4. 兼容性测试
- **不同设备测试**: 在不同设备上测试输入体验
- **不同浏览器测试**: 验证在不同浏览器中的表现
- **触摸输入测试**: 在移动设备上测试触摸输入

## 后续优化建议

### 1. 数值范围限制
- 可以添加最大值和最小值限制
- 防止输入过大或过小的数值

### 2. 业务规则验证
- 添加特定字段的业务规则验证
- 例如：某些费用不允许为负数

### 3. 用户提示优化
- 添加更详细的输入提示
- 提供负数价格的业务含义说明

### 4. 批量操作
- 支持批量设置价格
- 提供价格模板功能
