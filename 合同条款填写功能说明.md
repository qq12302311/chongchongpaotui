# 合同条款填写功能说明

## 功能概述
已成功将根目录的"充充服务商协议.docx"内容整合到`riderEnd/contract.vue`文件中，并添加了可填写的表单字段。

## 主要修改内容

### 1. 合同内容更新
- 将原有的简化合同条款替换为完整的服务商合作协议内容
- 包含8个主要条款：合作内容、服务标准、费用结算、权利义务、违约责任、协议变更、争议解决、其他条款

### 2. 新增填写字段
在合同中添加了以下可填写字段：
- **服务商姓名** (`contractData.serviceProviderName`)
- **身份证号** (`contractData.idNumber`) 
- **联系电话** (`contractData.phoneNumber`)
- **服务区域** (`contractData.serviceArea`)

### 3. 表单验证
- 添加了必填字段验证
- 只有在填写完所有必填信息、同意条款并完成签名后才能签署合同
- 通过`canSign`计算属性控制签署按钮状态

### 4. 数据持久化
- 合同数据保存到本地存储(`userInfo.contractData`)
- 已签署合同会显示填写的信息
- 未签署时会从用户信息预填充部分数据

### 5. 界面优化
- 添加了专门的合同信息填写区域
- 美化了输入框样式
- 已签署状态下输入框变为只读
- 填写的信息在合同条款中动态显示

### 6. 合同生成优化
- 更新了`generateContractImage`方法
- 生成的电子合同图片包含填写的个人信息
- 合同图片格式更加规范和完整

## 使用说明

### 用户操作流程
1. 打开合同页面
2. 阅读合同条款
3. 填写个人信息（服务商姓名、身份证号、联系电话、服务区域）
4. 勾选同意条款
5. 手写签名
6. 点击"确认签署合同"

### 技术实现要点
- 使用Vue.js响应式数据绑定
- 表单验证确保数据完整性
- Canvas技术生成包含个人信息的电子合同
- 本地存储保存合同数据和签署状态

## 文件结构
```
riderEnd/contract.vue
├── 模板部分 (template)
│   ├── 合同信息卡片
│   ├── 合同内容展示
│   │   ├── 个人信息填写表单
│   │   └── 完整合同条款
│   ├── 签名区域
│   └── 隐藏的合同生成画布
├── 脚本部分 (script)
│   ├── 数据定义 (contractData)
│   ├── 计算属性 (canSign)
│   ├── 生命周期方法
│   └── 业务方法
└── 样式部分 (style)
    ├── 表单样式
    ├── 合同内容样式
    └── 响应式布局
```

## 注意事项
- 确保所有必填字段都已填写才能签署
- 已签署的合同信息不可修改
- 合同数据会保存在本地存储中
- 生成的电子合同包含完整的个人信息和签名
