# 提现账户管理功能实现说明

## 功能概述

在 `riderEnd/withdraw.vue` 中实现了完整的提现账户管理功能，用户可以选择使用不同的提现方式（支付宝或银行卡），并且可以新增和修改提现账户。

## 主要功能特性

### 1. 提现账户管理页面 (`withdraw-account-manage.vue`)
- **当前账户显示**: 显示已设置的提现账户信息
- **账户类型选择**: 支持选择支付宝或银行卡作为提现方式
- **新增账户**: 可以添加新的支付宝或银行卡账户
- **编辑账户**: 可以修改现有的提现账户信息
- **账户信息脱敏**: 安全显示账户号码，保护用户隐私

### 2. 支持的提现方式
- **支付宝**: 实时到账，单笔限额5万元
- **银行卡**: 1-3个工作日到账，单笔限额10万元

### 3. 用户界面优化
- **直观的账户选择**: 清晰的图标和说明
- **安全的信息显示**: 账户号码脱敏处理
- **便捷的操作流程**: 一键添加和编辑账户

## 页面结构

### 提现账户管理页面功能模块

#### 1. 当前提现账户区域
```vue
<view class="current-account-section" v-if="hasWithdrawAccount">
  <view class="account-card">
    <!-- 账户类型图标和名称 -->
    <!-- 脱敏后的账户号码 -->
    <!-- 账户持有人姓名 -->
    <!-- 编辑和选择按钮 -->
  </view>
</view>
```

#### 2. 添加新账户区域
```vue
<view class="add-account-section">
  <!-- 支付宝选项 -->
  <view class="account-option" @click="addAccount('alipay')">
    <!-- 支付宝图标和说明 -->
  </view>
  
  <!-- 银行卡选项 -->
  <view class="account-option" @click="addAccount('bank')">
    <!-- 银行卡图标和说明 -->
  </view>
</view>
```

#### 3. 提现说明区域
```vue
<view class="withdraw-info">
  <!-- 提现规则和说明 -->
</view>
```

## 技术实现细节

### 1. 数据结构
```javascript
data() {
  return {
    withdrawInfo: null,           // 提现账户信息
    selectedAccountType: '',      // 当前选择的账户类型
    riderUserInfo: null          // 用户信息
  }
}
```

### 2. 核心计算属性

#### hasWithdrawAccount
```javascript
hasWithdrawAccount() {
  return this.withdrawInfo && (this.withdrawInfo.alipay_id || this.withdrawInfo.bank_card_number)
}
```

#### currentAccountType
```javascript
currentAccountType() {
  if (!this.withdrawInfo) return ''
  return this.withdrawInfo.alipay_id ? 'alipay' : 'bank'
}
```

#### maskedAccountNumber
```javascript
maskedAccountNumber() {
  if (this.currentAccountType === 'alipay') {
    const account = this.withdrawInfo.alipay_id || ''
    return account.slice(0, 3) + '****' + account.slice(-2)
  } else {
    const cardNumber = this.withdrawInfo.bank_card_number || ''
    return '**** **** **** ' + cardNumber.slice(-4)
  }
}
```

### 3. 核心方法

#### loadWithdrawInfo()
```javascript
loadWithdrawInfo() {
  const riderUserInfo = uni.getStorageSync('riderUserInfo') || {}
  this.riderUserInfo = riderUserInfo
  this.withdrawInfo = riderUserInfo.withdraw_info
  
  if (this.hasWithdrawAccount) {
    this.selectedAccountType = this.currentAccountType
  }
}
```

#### addAccount(type)
```javascript
addAccount(type) {
  uni.navigateTo({
    url: `/riderEnd/add-account?type=${type}`
  })
}
```

#### confirmSelection()
```javascript
confirmSelection() {
  // 返回上一页并传递选择的账户类型
  const pages = getCurrentPages()
  const prevPage = pages[pages.length - 2]
  
  if (prevPage) {
    prevPage.$vm.selectedAccountType = this.selectedAccountType
    prevPage.$vm.loadUserInfo()
  }
  
  uni.navigateBack()
}
```

## 页面间数据传递

### 1. 从提现页面到账户管理页面
```javascript
// withdraw.vue
goToEditAccount() {
  uni.navigateTo({
    url: '/riderEnd/withdraw-account-manage'
  });
}
```

### 2. 从账户管理页面到添加账户页面
```javascript
// withdraw-account-manage.vue
addAccount(type) {
  uni.navigateTo({
    url: `/riderEnd/add-account?type=${type}`
  })
}
```

### 3. 页面间数据刷新
```javascript
// 添加/编辑成功后刷新上一页数据
const pages = getCurrentPages();
const prevPage = pages[pages.length - 2];
if (prevPage && prevPage.$vm.loadWithdrawInfo) {
  prevPage.$vm.loadWithdrawInfo();
}
```

## 提现页面优化

### 1. 动态显示账户类型
```javascript
// withdraw.vue 计算属性
withdrawAccountType() {
  if (!this.withdrawInfo) return '未设置';
  if (this.withdrawInfo.alipay_id) return '支付宝';
  if (this.withdrawInfo.bank_card_number) return '银行卡';
  return '未设置';
}
```

### 2. 统一的账户号码脱敏
```javascript
maskedAccountNumber() {
  if (this.withdrawInfo.alipay_id) {
    // 支付宝账号脱敏
    const account = this.withdrawInfo.alipay_id;
    return account.slice(0, 3) + '****' + account.slice(-2);
  } else if (this.withdrawInfo.bank_card_number) {
    // 银行卡号脱敏
    const cardNumber = this.withdrawInfo.bank_card_number;
    return cardNumber.slice(-4);
  }
  return '****';
}
```

### 3. 提现确认优化
```javascript
confirmWithdraw() {
  uni.showModal({
    title: '确认提现',
    content: `确认提现 ¥${this.withdrawAmount} 到${this.withdrawAccountType}(${this.maskedAccountNumber})？`,
    success: (res) => {
      if (res.confirm) {
        this.submitWithdraw();
      }
    }
  });
}
```

## 用户体验优化

### 1. 视觉设计
- **统一的主题色**: 使用 #2492F2 作为主色调
- **清晰的层次结构**: 合理的间距和分组
- **直观的图标**: 支付宝和银行卡使用对应的图标

### 2. 交互体验
- **一键操作**: 点击即可添加对应类型的账户
- **状态反馈**: 选择状态的视觉反馈
- **安全提示**: 提现确认时显示完整信息

### 3. 错误处理
- **参数验证**: URL参数类型验证
- **数据兼容**: 处理空数据和异常情况
- **用户提示**: 适当的成功和错误提示

## 安全特性

### 1. 数据脱敏
- 支付宝账号只显示前3位和后2位
- 银行卡号只显示后4位
- 保护用户隐私信息

### 2. 参数验证
- URL参数类型检查
- 数据存在性验证
- 防止异常操作

## 测试建议

1. **账户类型切换测试**: 测试支付宝和银行卡的切换
2. **数据脱敏测试**: 验证账户号码脱敏显示
3. **页面跳转测试**: 测试各页面间的跳转和数据传递
4. **数据刷新测试**: 验证添加/编辑后的数据刷新
5. **异常情况测试**: 测试无账户信息时的处理

## 后续优化建议

1. **多账户支持**: 支持用户添加多个提现账户
2. **默认账户设置**: 允许用户设置默认提现账户
3. **账户验证**: 添加账户真实性验证
4. **提现记录**: 显示历史提现记录
5. **手续费计算**: 实时显示提现手续费
