# 个人中心接口查询功能实现说明

## 功能概述

在 `riderEnd/my.vue` 中成功实现了每次进入页面时自动查询用户信息接口的功能，确保页面显示的数据始终是最新的。

## 主要功能特性

### 1. 自动数据更新
- **触发时机**: 每次进入个人中心页面（onShow生命周期）
- **接口地址**: `service/member/info`
- **更新频率**: 每次页面显示时都会查询

### 2. 接口参数
按照要求构建的请求参数：
```javascript
{
  "service_member_id": this.userInfo.id,
  "sign": md5(`service_member_id=${this.riderUserInfo.id}&phone_number=${this.riderUserInfo.phone}`),
  "member_id": this.userInfo.id
}
```

### 3. 数据更新字段
自动更新以下riderUserInfo对象字段：
- **balance**: 账户余额
- **month_commission**: 本月佣金
- **month_completed_tasks_count**: 本月完成任务数
- **today_commission**: 今日佣金  
- **today_completed_tasks_count**: 今日完成任务数
- **rate**: 佣金比例
- **zone**: 服务区域信息

## 技术实现细节

### 核心方法

#### 1. fetchUserInfo()
```javascript
async fetchUserInfo() {
  try {
    const riderUserInfo = uni.getStorageSync('riderUserInfo');
    
    // 构建签名
    const signStr = `service_member_id=${riderUserInfo.id}&phone_number=${riderUserInfo.phone}`;
    const sign = md5(signStr);
    
    // 构建请求参数
    const params = {
      service_member_id: riderUserInfo.id,
      sign: sign,
      member_id: riderUserInfo.id
    };
    
    // 调用接口
    const res = await this.$request('service/member/info', params, 'POST');
    
    if (res.code === 200 && res.data) {
      // 更新用户信息
      const updatedUserInfo = {
        ...riderUserInfo,
        balance: res.data.balance || riderUserInfo.balance || 0,
        month_commission: res.data.month_commission || riderUserInfo.month_commission || 0,
        month_completed_tasks_count: res.data.month_completed_tasks_count || riderUserInfo.month_completed_tasks_count || 0,
        today_commission: res.data.today_commission || riderUserInfo.today_commission || 0,
        today_completed_tasks_count: res.data.today_completed_tasks_count || riderUserInfo.today_completed_tasks_count || 0,
        rate: res.data.rate || riderUserInfo.rate || 1,
        zone: res.data.zone || riderUserInfo.zone || null
      };
      
      // 保存到本地存储
      uni.setStorageSync('riderUserInfo', updatedUserInfo);
      
      // 更新页面显示
      this.userInfo = updatedUserInfo;
    }
  } catch (error) {
    console.error('查询用户信息失败:', error);
  }
}
```

#### 2. 修改后的checkLoginStatus()
```javascript
checkLoginStatus() {
  const storedUserInfo = uni.getStorageSync('riderUserInfo');
  
  if (storedUserInfo) {
    this.isLoggedIn = true;
    this.userInfo = storedUserInfo;
    
    // 判断管理员权限
    if (storedUserInfo.level && (storedUserInfo.level >= 9)) {
      this.isAdmin = true;
    } else {
      this.isAdmin = false;
    }
    
    // 如果已登录，查询最新的用户信息
    this.fetchUserInfo();
  } else {
    // 未登录状态处理
    this.isLoggedIn = false;
    this.isAdmin = false;
    this.userInfo = {
      name: '',
      id: '',
      phone: '',
      verified: false,
      depositPaid: false
    };
  }
}
```

### 生命周期管理
```javascript
onShow() {
  // 每次显示页面时检查登录状态并获取最新信息
  this.checkLoginStatus();
}
```

## 数据流程

1. **页面显示** → 触发onShow生命周期
2. **检查登录状态** → 调用checkLoginStatus()
3. **如果已登录** → 自动调用fetchUserInfo()
4. **构建请求参数** → 包含签名验证
5. **调用接口** → service/member/info
6. **更新数据** → 更新本地存储和页面显示

## 错误处理

### 1. 参数验证
- 检查用户ID和手机号是否存在
- 缺少必要参数时记录错误日志并返回

### 2. 接口异常
- 使用try-catch捕获网络错误
- 不显示错误提示，避免影响用户体验
- 记录详细的错误日志便于调试

### 3. 数据兼容性
- 使用默认值处理接口返回的空数据
- 保留原有数据作为备选值

## 安全特性

### 1. MD5签名验证
- 使用service_member_id和phone_number构建签名字符串
- 通过MD5加密确保请求安全性

### 2. 数据合并策略
- 使用扩展运算符保留原有数据
- 新数据优先，原有数据作为备选

## 用户体验优化

### 1. 静默更新
- 后台查询，不显示loading状态
- 不干扰用户正常操作

### 2. 数据一致性
- 同时更新本地存储和页面显示
- 确保数据在应用内保持一致

### 3. 性能考虑
- 只在页面显示时查询，避免不必要的请求
- 异步处理，不阻塞页面渲染

## 测试建议

1. **登录状态测试**: 测试已登录和未登录状态的处理
2. **接口响应测试**: 测试正常响应和异常响应的处理
3. **数据更新测试**: 验证页面数据是否正确更新
4. **网络异常测试**: 测试网络错误时的处理
5. **数据兼容性测试**: 测试接口返回不完整数据时的处理

## 注意事项

1. 确保用户已登录且有有效的ID和手机号
2. 接口调用失败不会影响页面正常显示
3. 数据更新是增量式的，不会覆盖其他字段
4. 签名算法需要与后端保持一致
