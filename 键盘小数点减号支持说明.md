# 键盘小数点和减号支持功能说明

## 问题分析

在某些设备和浏览器上，`type="number"` 可能不会在键盘上显示小数点和减号，这是因为：

1. **不同平台的实现差异**：iOS、Android、不同浏览器对 `type="number"` 的键盘显示有不同的实现
2. **输入模式限制**：某些实现可能只显示纯数字键盘，不包含小数点和负号
3. **兼容性问题**：旧版本的浏览器或小程序可能不完全支持所有属性

## 解决方案

### 1. 使用 type="text" + inputmode="decimal" + pattern

```vue
<input 
  type="text" 
  v-model="currentSettings.bubao_base_device_fee" 
  class="item-input"
  @input="validateNumberInput($event, 'bubao_base_device_fee')"
  placeholder="数字/小数/负数"
  inputmode="decimal"
  pattern="[-]?[0-9]*[.]?[0-9]*" 
/>
```

### 2. 属性详细说明

#### type="text"
- **优势**: 最大兼容性，所有浏览器都支持
- **配合**: 与 `inputmode` 和 `pattern` 配合使用
- **灵活性**: 允许更精确的输入控制

#### inputmode="decimal"
- **作用**: 明确告诉浏览器显示十进制数字键盘
- **包含**: 数字0-9、小数点(.)、负号(-)
- **兼容性**: 现代浏览器和小程序都支持

#### pattern="[-]?[0-9]*[.]?[0-9]*"
- **作用**: 定义允许的输入格式
- **格式**: 可选的负号 + 数字 + 可选的小数点 + 数字
- **验证**: 浏览器会根据此模式进行基础验证

### 3. Pattern 正则表达式解析

```
[-]?[0-9]*[.]?[0-9]*
```

- `[-]?` - 可选的负号（0个或1个）
- `[0-9]*` - 零个或多个数字
- `[.]?` - 可选的小数点（0个或1个）
- `[0-9]*` - 零个或多个数字

**支持的格式**：
- `123` - 正整数
- `-123` - 负整数
- `12.34` - 正小数
- `-12.34` - 负小数
- `.5` - 省略整数部分的小数
- `-.5` - 省略整数部分的负小数

## 键盘显示效果

### 移动设备
- **iOS Safari**: 显示数字键盘，包含 0-9、小数点(.)、负号(-)
- **Android Chrome**: 显示十进制键盘，包含数字、小数点、负号
- **微信小程序**: 根据 `inputmode="decimal"` 显示对应键盘
- **支付宝小程序**: 显示数字键盘，包含小数点和负号

### 桌面设备
- **Chrome/Firefox/Safari**: 普通文本输入，但会根据 `pattern` 进行验证
- **Edge**: 支持 `inputmode` 提示，在触摸设备上显示数字键盘

## 验证逻辑

由于使用了 `type="text"`，我们需要保持完整的验证逻辑：

```javascript
validateNumberInput(event, fieldName) {
    let value = event.detail.value;
    
    // 确保只包含数字、负号和小数点
    value = value.replace(/[^-\d.]/g, '');
    
    // 处理负号：只能在开头出现一次
    if (value.includes('-')) {
        const firstChar = value.charAt(0);
        value = value.replace(/-/g, '');
        if (firstChar === '-') {
            value = '-' + value;
        }
    }
    
    // 处理小数点：只能有一个
    const dotCount = (value.match(/\./g) || []).length;
    if (dotCount > 1) {
        const firstDotIndex = value.indexOf('.');
        const beforeDot = value.substring(0, firstDotIndex + 1);
        const afterDot = value.substring(firstDotIndex + 1).replace(/\./g, '');
        value = beforeDot + afterDot;
    }
    
    // 更新值
    this.currentSettings[fieldName] = value;
}
```

## 兼容性对比

### type="number" 方案
| 平台 | 数字键盘 | 小数点 | 负号 | 兼容性 |
|------|----------|--------|------|--------|
| iOS Safari | ✅ | ❌ | ❌ | 部分支持 |
| Android Chrome | ✅ | ✅ | ❌ | 部分支持 |
| 微信小程序 | ✅ | ❌ | ❌ | 部分支持 |
| 桌面浏览器 | ✅ | ✅ | ✅ | 完全支持 |

### type="text" + inputmode="decimal" 方案
| 平台 | 数字键盘 | 小数点 | 负号 | 兼容性 |
|------|----------|--------|------|--------|
| iOS Safari | ✅ | ✅ | ✅ | 完全支持 |
| Android Chrome | ✅ | ✅ | ✅ | 完全支持 |
| 微信小程序 | ✅ | ✅ | ✅ | 完全支持 |
| 桌面浏览器 | ✅ | ✅ | ✅ | 完全支持 |

## 测试建议

### 1. 不同设备测试
- **iPhone**: 测试 Safari 和微信小程序
- **Android**: 测试 Chrome 和各种小程序
- **iPad**: 测试触摸键盘显示
- **桌面**: 测试各种浏览器

### 2. 键盘功能测试
- 确认键盘包含数字 0-9
- 确认键盘包含小数点 (.)
- 确认键盘包含负号 (-)
- 测试键盘切换是否流畅

### 3. 输入验证测试
- 测试正常数字输入
- 测试小数点输入
- 测试负数输入
- 测试非法字符过滤

## 优势总结

### 1. 最大兼容性
- 在所有平台上都能显示包含小数点和负号的键盘
- 不依赖特定浏览器或平台的实现

### 2. 用户体验
- 用户可以直接在键盘上输入小数点和负号
- 减少输入错误和操作步骤
- 提高输入效率

### 3. 技术可靠性
- 使用标准的HTML5属性
- 有完整的验证逻辑作为后备
- 代码简洁易维护

## 注意事项

### 1. 浏览器支持
- `inputmode` 属性在现代浏览器中支持良好
- 旧版浏览器会忽略此属性，但不影响功能

### 2. 小程序兼容
- 不同小程序平台对 `inputmode` 的支持可能有差异
- 建议在目标平台上进行充分测试

### 3. 用户教育
- 可以在界面上提供简单的使用说明
- 通过占位符文本提示用户可以输入的格式

这个方案应该能够在绝大多数设备和平台上正确显示包含小数点和负号的数字键盘。
